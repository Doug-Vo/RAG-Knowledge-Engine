# Run the Flask app locally

Start the development server from the correct working directory.

```bash
cd "Main Webpage" && py -3.12 app.py
```

Serves at http://127.0.0.1:5000. Requires `.env` with `MONGO_URI`, `OPENAI_API_KEY`, and `SECRET_KEY`.
